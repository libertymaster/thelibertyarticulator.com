bind 0.0.0.0
port 6379
protected-mode yes
aclfile /run/secrets/redis_acl
dir /data
appendonly yes
appendfsync everysec
save 900 1
maxmemory 256mb
# Shared broker/cache: never silently evict queued tasks.
maxmemory-policy noeviction
loglevel notice
logfile ""
